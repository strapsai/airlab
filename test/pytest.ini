[pytest]
testpaths = unit integration e2e install
markers =
    unit: pure-logic tests, no container/network (T1)
    integration: git-fixture tests, local only (T2)
    e2e: container / real-hardware tests (T3)
    install: package build+install tests (T4)
addopts = -ra
